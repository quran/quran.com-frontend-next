---
description: Refresh Jira ticket data and update SPEC.md
---

# Sync Ticket Workflow

1. Fetch ticket recursively (`fetch-jira-ticket`)
2. Compile related tickets (`compile-related-tickets`)
3. Regenerate SPEC (`generate-spec-document`)
