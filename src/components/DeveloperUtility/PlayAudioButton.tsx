import Button from '../dls/Button/Button';

const PlayAudioButton: React.FC = () => {
  return (
    <Button
      onClick={() => {
        // nothing
      }}
      text="Play audio"
    />
  );
};

export default PlayAudioButton;
