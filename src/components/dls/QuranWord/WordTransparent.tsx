import styled from 'styled-components';

const WordTransparent = styled.i`
  position: absolute;
  right: 0;
  top: 0;
  bottom: 0;
  color: transparent;
  -webkit-user-select: text;
  -moz-user-select: text;
  -ms-user-select: text;
  user-select: text;
`;

export default WordTransparent;
